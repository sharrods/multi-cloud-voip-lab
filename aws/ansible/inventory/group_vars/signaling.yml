---
# Signaling server configuration
server_role: signaling

# OpenSIPS configuration
opensips_version: "3.4"
opensips_listen_port: 5060

# Redis configuration
redis_version: "7-alpine"
redis_password: "VoipLab2026"
redis_maxmemory: "50mb"
redis_maxmemory_policy: "allkeys-lru"

# Services to deploy
services:
  - opensips
  - redis
  - node_exporter
  - redis_exporter
