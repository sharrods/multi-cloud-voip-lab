---
# Media server configuration
server_role: media

# FreeSWITCH configuration
freeswitch_version: "latest"
freeswitch_rtp_start: 10000
freeswitch_rtp_end: 20000

# RTPEngine configuration
rtpengine_version: "latest"
rtpengine_listen_ip: "{{ private_ip }}"

# Redis configuration
redis_version: "7-alpine"
redis_password: "VoipLab2026"
redis_maxmemory: "50mb"

# Services to deploy
services:
  - freeswitch
  - rtpengine
  - redis
  - node_exporter
