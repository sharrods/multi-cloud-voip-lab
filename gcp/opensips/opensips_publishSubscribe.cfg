log_level=4
stderror_enabled=yes
syslog_enabled=no
mpath="/usr/lib/x86_64-linux-gnu/opensips/modules/"
auto_aliases=no
udp_workers=4
socket=udp:10.128.0.4:5060 as 34.44.206.214:5060

loadmodule "signaling.so"
loadmodule "sl.so"
loadmodule "tm.so"
loadmodule "rr.so"
loadmodule "maxfwd.so"
loadmodule "sipmsgops.so"
loadmodule "textops.so"
loadmodule "mi_fifo.so"
loadmodule "proto_udp.so"
loadmodule "dialog.so"
loadmodule "rtpengine.so"
loadmodule "pike.so"
loadmodule "usrloc.so"
loadmodule "registrar.so"

modparam("mi_fifo", "fifo_name", "/tmp/opensips_fifo")
modparam("rr", "append_fromtag", 1)
modparam("tm", "fr_timeout", 30)
modparam("tm", "fr_inv_timeout", 120)
modparam("dialog", "default_timeout", 21600)
modparam("dialog", "dlg_match_mode", 1)
modparam("pike", "sampling_time_unit", 10)
modparam("pike", "reqs_density_per_unit", 100)
modparam("rtpengine", "rtpengine_sock", "udp:127.0.0.1:2223")
modparam("usrloc", "db_mode", 0)
modparam("registrar", "default_expires", 3600)

route {
    if (!mf_process_maxfwd_header(10)) {
        sl_send_reply(483, "Too Many Hops");
        exit;
    }
    if (is_method("OPTIONS")) {
        sl_send_reply(200, "OK");
        exit;
    }

    # FIX 1: Terminate PUBLISH/SUBSCRIBE locally — never relay these
    if (is_method("PUBLISH|SUBSCRIBE")) {
        sl_send_reply(501, "Not Implemented");
        exit;
    }

    if (has_totag()) {
        if (loose_route()) {
            if (is_method("BYE")) {
                rtpengine_delete();
            }
            # FIX 2: Only relay legitimate in-dialog methods
            if (!is_method("ACK|BYE|INVITE|UPDATE|INFO|PRACK")) {
                sl_send_reply(501, "Not Implemented");
                exit;
            }
            t_relay();
            exit;
        }
        if (match_dialog()) {
            if (is_method("BYE")) {
                rtpengine_delete();
            }
            t_relay();
            exit;
        }
        if (is_method("ACK")) {
            if (t_check_trans()) {
                t_relay();
                exit;
            }
            exit;
        }
        sl_send_reply(404, "Not Here");
        exit;
    }
    if (is_method("CANCEL")) {
        if (t_check_trans()) {
            t_relay();
        }
        exit;
    }
    t_check_trans();
    if (is_method("REGISTER")) {
        if (!save("location")) {
            sl_reply_error();
        }
        exit;
    }
    if (is_method("INVITE")) {
        record_route();
        create_dialog();
        $du = "sip:10.128.0.4:5080";
        if (has_body("application/sdp")) {
            rtpengine_offer("replace-origin replace-session-connection ICE=remove RTP/AVP internal external");
        }
        t_on_reply("1");
        if (!t_relay()) {
            sl_reply_error();
        }
        exit;
    }
    t_relay();
}

onreply_route[1] {
    if (has_body("application/sdp")) {
        rtpengine_answer("replace-origin replace-session-connection ICE=remove RTP/AVP external internal");
    }
}
